export const LANDING_REVEAL_CLASS = "opacity-0 translate-y-10";
export const LANDING_CARD_CLASS = "opacity-0 translate-y-10";

/** Section headings - matches homepage typography (Unbounded). */
export const LANDING_HEADING_CLASS = "font-heading font-bold tracking-tight";

/** Centered section title sizes (pair with LANDING_HEADING_CLASS). */
export const LANDING_SECTION_TITLE_CENTER_CLASS =
  "mx-auto max-w-3xl text-center text-3xl sm:text-4xl md:text-5xl";

/** Left-aligned section title sizes (pair with LANDING_HEADING_CLASS). */
export const LANDING_SECTION_TITLE_LEFT_CLASS = "max-w-md text-3xl md:text-5xl";

/** Body copy - matches homepage paragraph typography. */
export const LANDING_BODY_CLASS = "text-lg leading-relaxed text-muted-foreground";

/** Frosted glass surface - nav bar, pills, feature chips. */
export const LANDING_GLASS_CLASS =
  "border border-white/50 bg-background/50 shadow-sm shadow-black/5 backdrop-blur-md supports-backdrop-filter:bg-background/40";

/** Smaller glass accent (icon badges on glass pills). */
export const LANDING_GLASS_ACCENT_CLASS =
  "border border-white/40 bg-primary/15 backdrop-blur-sm";
